# CampSpark

CampSpark is a modern social experience built with Next.js, Supabase, and Gemini AI. It combines a social feed, messaging, and a cozy AI companion called Marshmallow. The app also supports local RAG ingestion via a Python FastAPI service for richer personalized AI responses.

## Features

- Authentication and profile handling with Supabase
- Protected routes enforced through middleware
- Social feed with posts, likes, saved items, and notifications
- Direct messaging support
- AI assistant endpoint powered by Google Gemini
- Vector search / RAG context for enhanced AI responses
- Local RAG ingestion service using FastAPI, Chroma, and Gemini embeddings
- Password update flow for email/password users

## Tech Stack

- `next` 16.2.4
- `react` 19.2.4
- `typescript`
- `tailwindcss` 4
- `@supabase/supabase-js` and `@supabase/ssr`
- `@google/generative-ai`
- FastAPI + LangChain + Chroma for local RAG service

## Repository Structure

- `src/app` - Next.js App Router pages and layouts
- `src/app/api` - API route handlers
- `src/utils/supabase` - Supabase server-side client utilities
- `src/lib/marshmallow-sync.ts` - Browser helper for local RAG ingestion
- `src/proxy.ts` - Middleware for session/profile enforcement
- `marshmellow/main.py` - Local FastAPI service for document ingestion and query
- `package.json` - project dependencies and scripts

## Local Development

### 1. Install dependencies

```bash
npm install
```

### 2. Configure environment variables

Create a `.env` file with the following values:

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
GEMINI_API_KEY=your-gemini-api-key
```

### 3. Run the Next.js app

```bash
npm run dev
```

Open `http://localhost:3000`.

### 4. Run the local Marshmallow RAG service (optional)

From the `marshmellow` folder:

```bash
pip install -r requirements.txt
python main.py
```

The service listens on `http://localhost:8000` and supports `/ingest` and `/ask` endpoints.

## AI and RAG Architecture

### Server-side AI assistant

- `src/app/api/marshmallow/route.ts` handles chat requests
- Authenticated users send prompts to the endpoint
- The route generates embeddings via Gemini and executes RAG search
- RAG context is built from relevant posts, recent messages, and fallback content
- The final response is generated by Gemini and stored in `chatbot_messages`

### Embedding sync

- `src/app/api/marshmallow/sync/route.ts` syncs post embeddings into Supabase
- It uses `marshmallow_index` for vector storage and retrieval
- Missing post embeddings are generated and upserted automatically

### Local RAG ingest flow

- `src/lib/marshmallow-sync.ts` sends content to `http://localhost:8000/ingest`
- `marshmellow/main.py` stores documents in Chroma with Gemini embeddings
- Local queries can be made via the `/ask` route for personalized multi-user retrieval

## Notes

- Middleware in `src/proxy.ts` protects routes and redirects unauthenticated users to `/signup`
- The app depends on both public and service role Supabase keys
- Password reset/change is implemented in `src/app/api/settings/password/route.ts`

## License

This repository does not include a license file. Add one if you want to make the project open source.
